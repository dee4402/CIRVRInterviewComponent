using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//used by the CreateDataCSV script to create the full report csv
public class FullReportInfo
{
    public string questionNum, ts, responseLength, interruptions, eyes, nose,
        mouth, cheeks, forehead, desk, monitor, keyboard, lamp, window, clock, picture, mouse, emotion; 
}
